# robots.txt for yearinreview.online
# This file tells search engines which pages they can and cannot crawl

# Allow all search engines to crawl the site
User-agent: *
Allow: /

# Disallow API endpoints (server-side only, no need to index)
Disallow: /api/

# Disallow payment-related pages (these are user-specific and shouldn't appear in search results)
Disallow: /success
Disallow: /cancel

# Point search engines to your sitemap for better indexing
Sitemap: https://yearinreview.online/sitemap.xml
